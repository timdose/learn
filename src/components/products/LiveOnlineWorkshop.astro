---
import PriceButton from '../PriceButton.astro';

interface Props {
  price: number;
  discount?: number;
  registrationOpen?: boolean;
  purchaseReturnUrl?: string;
  pageTitle: string;
  pageUrl: string;
  askTimes?: boolean;
}
const props = Astro.props;
---
<div class="pricing-option container columns">
  <div class="wrapper">
    <div class="inner">
      <div>
        <h1 class="style6">Live Online Workshop</h1>
        <div class="list">
          <ul>
            <li><p>Take the class live over Zoom</p></li>
            <li><p>Ask questions in real time</p></li>
            <li><p>Access to video recordings until 1 week after the last class</p></li>
            <li><p>Connect with other students on Tim's private Discord server</p></li>
            <li><p><strong>8 seats only—first come, first serve</strong></p></li>
          </ul>
        </div>
      </div>
      <div>
        <PriceButton
          price={props.price}
          discount={props.discount}
          itemName="Live Online Workshop"
          registrationOpen={props.registrationOpen}
          purchaseReturnUrl={props.purchaseReturnUrl}
          pageTitle={props.pageTitle}
          pageUrl={props.pageUrl}
          askTimes={props.askTimes}
        />
      </div>
    </div>
  </div>
</div>
