{
  "scripts": {
    "start": "bundle exec jekyll serve --livereload --host 0.0.0.0",
    "build": "JEKYLL_ENV=production bundle exec jekyll build --destination _build",
    "deploy": "npm run build && rsync -avz --delete _build/ dh_uasa28@vps61088.dreamhostps.com:/home/dh_uasa28/learn.timdoseart.com/",
    "test": "npx cypress open",
    "test:waitlist": "bin/test-waitlist",
    "test:time-preference": "bin/test-time-preference"
  },
  "devDependencies": {
    "cypress": "^13.15.1"
  }
}
